This code implements inventory model which is a model of a store which must choose how much of discrete good is to be ordered. The plots show how solver approaches the solution
(an optimal ordering policy) and resulting utility as iterations progress.
